#!/usr/bin/env bash
set -euo pipefail

rand=$(head /dev/urandom | tr -dc A-Za-z0-9 | head -c 6 ; echo '')

cat > $HOME/.local/share/applications/userapp-"${2}"-${rand}.desktop <<EOF
[Desktop Entry]
Type=Application
Exec=${1}
Name=${2}
EOF
