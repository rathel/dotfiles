{
  "layer": "top",
  "position": "top",
  "modules-center": ["custom/clock", "network"],
  "modules-right": ["tray"],

  "custom/clock": {
    "exec": "date '+%m/%d %H:%M'",
    "interval": 60
  },

  "tray": {
  	"icon-size": 18,
	"spacing": 10
  },

  "network": {
    "interface": "auto", // or "wlan0" / "eth0"
    "format": "{ifname}: {bandwidthDownBytes} ↓↑ {bandwidthUpBytes}",
    "format-disconnected": "Disconnected",
    "max-length": 50
  }
}